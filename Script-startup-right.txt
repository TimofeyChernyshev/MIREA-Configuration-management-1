# Stage 4. ls command
ls
ls /
ls test_vfs/dir1

# Stage 4. cd command
cd test_vfs
cd /
cd test_vfs/dir1
cd ..
cd ..
cd /test_vfs/dir2/subdir1
cd .

# Stage 4. uniq command
cd /
uniq test_vfs/dir1/file1.txt
cd test_vfs/dir1
uniq file1.txt

# Stage 4. tail command
cd /
tail Script-startup-right.txt
tail Script-startup-right.txt Script-startup-wrong.txt
tail -n 3 Script-startup-right.txt
tail -n 3 Script-startup-right.txt Script-startup-wrong.txt

# Stage 4. vfs-save
vfs-save test_2

# Stage 5. mv command - переименование файла
mv test_vfs/dir1/file1.txt test_vfs/dir1/renamed_file.txt

# Stage 5. mv command - перемещение файла в директорию
mv test_vfs/dir1/file2.txt test_vfs/

# Stage 5. mv command - переименование директории
mv test_vfs/dir1 test_vfs/dir5

# Stage 5. mv command - перемещение директории
mv test_vfs/dir3 test_vfs/dir2/
vfs-save saved-vfs

# Stage 5. chown command - изменение владельца файла
chown admin test_vfs/dir5/renamed_file.txt

# Stage 5. chown command - изменение владельца директории
chown root test_vfs/dir2/

# Stage 5. chown command - несколько файлов
chown user test_vfs/root1.txt test_vfs/root2.txt