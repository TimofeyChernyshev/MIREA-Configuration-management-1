fake_command 321

# use vfs-save/vfs-load command without argument
vfs-save

# Stage 4. ls to file, cd to file
ls /test_vfs/dir1/file1.txt
cd /test_vfs/dir1/file1.txt

# Stage 4. ls, cd to wrong directory
ls "123 123"
cd 123

# Stage 4. uniq, tail without arguments
uniq
tail

# Stage 4. tail wrong -n paramater
tail -n -12 /test_vfs/dir1/file1.txt
tail -n 0 /test_vfs/dir1/file1.txt

# Stage 4. tail -n without path
tail -n 5

# Stage 4. tail -n without number
tail -n /test_vfs/dir1/file1.txt

# Stage 4. tail to directory
tail /test_vfs/dir1

# Stage 5. mv без аргументов
mv

# Stage 5. mv с недостаточным количеством аргументов
mv file.txt

# Stage 5. mv несуществующего файла
mv non_existent.txt somewhere.txt

# Stage 5. mv в существующий файл
mv test_vfs/dir1/file1.txt test_vfs/root1.txt

# Stage 5. mv в самого себя
mv test_vfs/dir1/file1.txt test_vfs/dir1/file1.txt
mv test_vfs/dir1 test_vfs/dir1

# Stage 5. chown без аргументов
chown

# Stage 5. chown с недостаточным количеством аргументов
chown admin

# Stage 5. chown несуществующего файла
chown admin non_existent.txt

# Stage 5. chown несуществующего пользователя
chown nonexistentuser file.txt

# Stage 5. mv в несуществующую директорию
mv test_vfs/dir1/file1.txt non_existent_dir/

# Stage 5. mv нескольких файлов в файл (а не директорию)
mv test_vfs/dir1/file1.txt test_vfs/dir1/file2.txt test_vfs/root1.txt